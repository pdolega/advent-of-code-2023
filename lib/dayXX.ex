defmodule DayXX do
  def calculate1(input) do
    lines = parseInput(input)
    :ok
  end

  def calculate2(input) do
    lines = parseInput(input)
    :ok
  end

  defp parseInput(input) do
    String.split(input, "\n")
  end
end
